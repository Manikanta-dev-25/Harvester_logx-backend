# ================= DATABASE =================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ================= SERVER =================
# Listen on all IP addresses (mobile/other devices)
server.address=0.0.0.0
server.port=8080

spring.application.name=FarmerBackend

# ================= EMAIL =================
# Brevo SMTP configuration
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=9a47d7001@smtp-brevo.com
spring.mail.password=bRTAMZI9Kfyp3FwV

# SMTP properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.required=false
spring.mail.properties.mail.smtp.connectiontimeout=40000
spring.mail.properties.mail.smtp.timeout=40000
spring.mail.properties.mail.smtp.writetimeout=40000

# Enable mail debug logs
logging.level.org.springframework.mail=DEBUG

# ================= ACTUATOR (Optional Health Check) =================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true
management.health.db.enabled=true
management.health.mail.enabled=true

# ================= CORS (Optional for Frontend Integration) =================
# Uncomment and configure if needed
# spring.web.cors.allowed-origins=https://your-frontend-url.github.io
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
# spring.web.cors.allowed-headers=*

# ================= LOGGING =================
logging.level.org.springframework.web=INFO
logging.level.com.yourpackage=DEBUG
====
spring.datasource.hikari.dataSourceProperties.ssl=true
spring.datasource.hikari.dataSourceProperties.sslmode=require
